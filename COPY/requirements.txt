# Detection Engineering toolchain
# Install with: pip install -r requirements.txt
#
# Python 3.10+ required.

# ── Core Sigma toolchain ──────────────────────────────────────────────────────

# sigma-cli: command-line interface for Sigma
#   Commands used: sigma check, sigma convert
#   https://github.com/SigmaHQ/sigma-cli
sigma-cli>=0.9.0

# pySigma: core Sigma library (sigma-cli depends on this)
#   https://github.com/SigmaHQ/pySigma
pySigma>=0.11.0

# ── pySigma backend: Microsoft / Sentinel / MDE ───────────────────────────────

# pySigma-backend-microsoft365defender
#   Provides:  -t kusto    (KQL / Kusto Query Language)
#   Pipelines: microsoft_xdr, microsoft_365_defender, sentinel_asim
#   Covers:    DeviceProcessEvents, DeviceRegistryEvents, DeviceFileEvents,
#              DeviceNetworkEvents (MDE Advanced Hunting tables) and
#              Sentinel ASIM-normalised tables.
#   https://github.com/SigmaHQ/pySigma-backend-microsoft365defender
pySigma-backend-microsoft365defender>=0.3.0

# ── pySigma pipelines ─────────────────────────────────────────────────────────

# pySigma-pipeline-sysmon
#   Provides field mapping from Sigma's generic logsource fields
#   (e.g. category: process_creation) to Sysmon field names.
#   Useful for validating rules against the Sysmon schema.
#   https://github.com/SigmaHQ/pySigma-pipeline-sysmon
pySigma-pipeline-sysmon>=1.0.0

# ── Utility ───────────────────────────────────────────────────────────────────

# PyYAML: used internally by sigma-cli and pySigma for YAML parsing.
# Pinned to avoid breaking changes in the Sigma library chain.
PyYAML>=6.0

# ── Development / local use only (not required for CI) ───────────────────────
pytest>=7.0

# Uncomment if you want these locally:
# rich        — prettier sigma-cli output in the terminal
# ipython     — interactive KQL / YAML debugging
